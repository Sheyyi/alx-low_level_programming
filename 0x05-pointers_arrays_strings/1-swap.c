#include "main.h"

/**
 * swap_int - Swaps the values of two integers
 * @a: A pointer to the integer a
 * @b: A pointer to the integer b
 *
 * Return: nothing.
 */
void swap_int(int *a, int *b)
{
	int n;

	n = *a;
	*a = *b;
	*b = m;
}
